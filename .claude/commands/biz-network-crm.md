---
name: biz-network-crm
description: "Set up downline & customer tracking for a network-marketing business"
argument-hint: "[action]"
---
Read and execute the command file at `commands/network-crm.md` in the project root.

Arguments: $ARGUMENTS

Follow the full process defined in that command file:
1. Read `commands/network-crm.md` for the command spec
2. Read `agents/04-deliver-agent.md` for the agent system prompt
3. Read `skills/network-marketing-crm/SKILL.md` for the skill instructions
4. Read relevant files in `skills/network-marketing-crm/references/`
5. Execute the command and deliver the output
