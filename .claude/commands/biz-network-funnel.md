---
name: biz-network-funnel
description: "Build invite scripts, qualification, and opportunity presentation for a network-marketing business"
argument-hint: "[step]"
---
Read and execute the command file at `commands/network-funnel.md` in the project root.

Arguments: $ARGUMENTS

Follow the full process defined in that command file:
1. Read `commands/network-funnel.md` for the command spec
2. Read `agents/02-attraction-agent.md` for the agent system prompt
3. Read `skills/network-marketing-funnel/SKILL.md` for the skill instructions
4. Read relevant files in `skills/network-marketing-funnel/references/`
5. Execute the command and deliver the output
